Build: mvn clean install -U


RUN

Main class: fractal.code.vending.Bootstrap

Program arguments:

1. amount - mandatory eg. 383
2. coin inventory file path - optional, if not provided the default infinite inventory will be used (test case 1) eg. /home/user/vending/coin-inventory.properties


VM arguments:

-Xss50m (the algorithm used is recursive so the stack size should be expanded to avoid StackOverflowError)
